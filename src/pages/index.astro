<!-- index.html -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>flimcadet // rashad wajahat</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- JetBrains Mono font from Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      background: #000;
      color: #fff;
      font-family: 'JetBrains Mono', monospace;
      font-size: 18px;
      font-weight: 400;
      line-height: 1.6;
      min-height: 100vh;
    }
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      padding: 40px 16px;
    }
    .container {
      width: 100%;
      max-width: 540px;
      display: flex;
      flex-direction: column;
      gap: 32px;
    }
    .header {
      margin-bottom: 8px;
    }
    .header-title,
    .header-roles,
    .contact,
    .contact a,
    ul,
    li,
    .arrow {
      font-size: 1em;
      font-weight: 400;
      color: #fff;
      letter-spacing: 0.01em;
    }
    .header-title {
      margin-bottom: 0;
    }
    .header-roles {
      margin-top: 4px;
    }
    .contact {
      margin-top: 8px;
    }
    .contact a {
      color: #fff;
      text-decoration: underline;
      transition: color 0.2s;
    }
    .contact a:hover {
      color: #fff;
    }
    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    li {
      margin-bottom: 6px;
    }
    .dropdown {
      cursor: pointer;
      user-select: none;
      display: flex;
      align-items: center;
      transition: background 0.2s;
      padding: 4px 0;
    }
    .dropdown:hover {
      background: #111;
    }
    .arrow {
      margin-right: 8px;
      transition: transform 0.2s;
      display: inline-block;
    }
    .dropdown.open .arrow {
      transform: rotate(90deg);
    }
    .dropdown-content {
      display: none;
      margin-left: 28px;
      margin-top: 2px;
      margin-bottom: 8px;
      color: #ccc;
      font-size: 0.97em;
      line-height: 1.5;
      max-width: 90%;
    }
    .dropdown.open .dropdown-content {
      display: block;
      animation: fadeIn 0.2s;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to   { opacity: 1; }
    }
    @media (max-width: 600px) {
      .container {
        max-width: 98vw;
        padding: 0 2vw;
      }
      body {
        padding: 24px 4px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="header">
      <div class="header-title">flimcadet // rashad wajahat</div>
      <div class="header-roles">writer // filmmaker // audiovisual artist</div>
      <div class="contact">
        <div>email <a href="mailto:rashad@flimcadet.com">rashad@flimcadet.com</a></div>
        <div>instagram <a href="https://instagram.com/flimcadet" target="_blank">@flimcadet</a></div>
      </div>
    </header>
    <ul>
      <li>
        <div class="dropdown">
          <span class="arrow">&gt;</span>
          [HXTCH]
        </div>
        <div class="dropdown-content">
          an electronic music festival outfit based in cox's bazar, bangladesh co-founded in 2021 by ishaq sikder, mridul moinak and rashad wajahat to share their love of electronic music with bangladeshi audiences. follow their events on instagram
          <a href="https://instagram.com/hxtch.me" target="_blank" style="color:#fff; text-decoration:underline;">@hxtch.me</a>.
        </div>
      </li>
      <li>
        <div class="dropdown">
          <span class="arrow">&gt;</span>
          short film
        </div>
        <div class="dropdown-content">
          Narrative and experimental short films exploring memory, identity, and perception.
        </div>
      </li>
      <li>
        <div class="dropdown">
          <span class="arrow">&gt;</span>
          audiovisual
        </div>
        <div class="dropdown-content">
          Immersive audiovisual works combining sound, video, and generative visuals for installations and live performance.
        </div>
      </li>
      <li>
        <div class="dropdown">
          <span class="arrow">&gt;</span>
          vj
        </div>
        <div class="dropdown-content">
          Live visual performance (VJing) for music events, blending real-time video and generative art.
        </div>
      </li>
    </ul>
  </div>
  <script>
    // Dropdown functionality
    document.querySelectorAll('.dropdown').forEach(function(drop) {
      drop.addEventListener('click', function() {
        // Close others
        document.querySelectorAll('.dropdown').forEach(function(other) {
          if (other !== drop) other.classList.remove('open');
        });
        // Toggle this one
        drop.classList.toggle('open');
      });
    });
  </script>
</body>
</html>
